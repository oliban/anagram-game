# Anagram Game - Production Environment Configuration Template
# NEVER commit actual production values to git

# Service Ports
GAME_SERVER_PORT=3000
WEB_DASHBOARD_PORT=3001
LINK_GENERATOR_PORT=3002
DATABASE_PORT=5432
ADMIN_SERVICE_PORT=3003

# Production Host Configuration
PRODUCTION_HOST=your-domain.com
AWS_ALB_URL=anagram-staging-alb-1354034851.eu-west-1.elb.amazonaws.com

# Database Configuration (Use strong passwords!)
DB_NAME=anagram_game
DB_USER=anagram_prod_user
DB_PASSWORD=$(openssl rand -base64 32)  # Generate secure password
DB_SSL=true
DB_SSL_REJECT_UNAUTHORIZED=true

# Environment
NODE_ENV=production

# Security Configuration (REQUIRED in production)
ADMIN_API_KEY=$(openssl rand -base64 32)  # Generate secure key
JWT_SECRET=$(openssl rand -base64 32)  # For future JWT implementation

# Security Settings (Production values)
SECURITY_RELAXED=false  # MUST be false in production
LOG_SECURITY_EVENTS=true  # Enable security audit logging
SKIP_RATE_LIMITS=false  # Never skip in production

# Production-only settings
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://app.your-domain.com
MAX_REQUESTS_PER_WINDOW=100
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes